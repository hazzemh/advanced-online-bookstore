spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bookstore_db
    username: postgres
    password: YarabSatrak2001
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect